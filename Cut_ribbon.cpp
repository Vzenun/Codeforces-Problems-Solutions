#include<iostream>
#include<string>
#include<cmath>
#include<climits>

using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    long long int n;
    long long int arr[3];
    cin>>n>>arr[0]>>arr[1]>>arr[2];
    long long int min=4000001;
    long long int max=0;
    long long int mid=;
    for(int i=0;i<3;i++){
        for(int j=i;j<3;j++){
            if(arr[i]>arr[j]){
                long long int temp=
            }
        }
    }
    if(min>b){
        min=b;
    }
    if(min>c){
        min=c;
    }
    if(max<b){
        max=b;
    }
    if(max<c){
        max=c;
    }
    )
    
    return 0;
}